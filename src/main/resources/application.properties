# ===============================
# = DATA SOURCE
# ===============================

# Set here configurations for the database connection

spring.datasource.url = jdbc:h2:file:/tmp/jku-lan-se;FILE_LOCK=FS;PAGE_SIZE=1024;CACHE_SIZE=8192
spring.datasource.username = sa
spring.datasource.password = sa
spring.datasource.driverClassName = org.h2.Driver

# Connection url for the database "netgloo_blog"
#spring.datasource.url = jdbc:postgresql://localhost:5432/postgres

# Username and password
#spring.datasource.username = postgres
#spring.datasource.password = postgres

# Keep the connection alive if idle for a long time (needed in production)
#spring.datasource.testWhileIdle = true
#spring.datasource.validationQuery = SELECT 1

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
#spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
#spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Allows Hibernate to generate SQL optimized for a particular DBMS
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

security.passwordSalt = 2ef7b5062a020827981a0f5e54112d83

### Logging
logging.level.org.springframework.web = INFO
logging.level.org.hibernate = ERROR
logging.level.jkulan = TRACE
security.user.password = password

security.saml.keystorePath = classpath:/saml/samlKeystore.jks
security.saml.keystorePassword = jku123
security.saml.keyName = sp
security.saml.entityId = urn:oeh:jku:lan:sp
security.saml.aconeturl = http://eduid.at/md/aconet-registered.xml
security.saml.adminRoleRegexp = ^https://lan[.]oeh[.]jku[.]at/admin$

# wro4j config
wro.preProcessors = lessCssImport
wro.postProcessors = less4j
#,cssVariables,cssMinJawr,jsMin
# set to 1 second to check for resource changes
wro.resourceWatcherUpdatePeriod = 0
wro.ignoreMissingResources = false

application.url = /
